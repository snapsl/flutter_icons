name: simple_icons
description: "Simple icons package"
publish_to: none
version: 0.22.0
repository: https://github.com/snapsl/flutter_icons
issue_tracker: https://github.com/snapsl/flutter_icons/issues

topics:
  - icons

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_utils:

flutter:
  fonts:
    - family: SimpleIcons
      fonts:
        - asset: fonts/SimpleIcons.ttf
